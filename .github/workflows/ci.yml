name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: >-
          git make gcc mingw-w64-x86_64-gcc
          mingw-w64-x86_64-winpthreads-git
          mingw-w64-x86_64-wininet-git
          mingw-w64-x86_64-pkg-config
    
    - name: Build & Test
      shell: msys2 {0}
      run: |
        make clean
        make all
        make test
        make integration

  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSYS2 (Windows)
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: >-
          git make gcc mingw-w64-x86_64-gcc
          mingw-w64-x86_64-winpthreads-git
          mingw-w64-x86_64-wininet-git
    
    - name: Build & Test
      shell: msys2 {0}
      run: |
        make clean
        make all
        make test
        make integration
